// Optional Firebase integration (push notifications / storage).
// Keep as a stub so the backend boots without Firebase credentials.
function getFirebaseAdmin() {
  return null;
}

module.exports = { getFirebaseAdmin };

