PORT=5000
NODE_ENV=development

# JWT
JWT_SECRET=change_me
JWT_EXPIRES_IN=7d

# SQLite (default). You can swap to Postgres/MySQL later.
DB_STORAGE=./data/dev.sqlite

# Optional integrations
OPENWEATHER_API_KEY=
ML_API_URL=http://localhost:8000

